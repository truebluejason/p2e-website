{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "diagnostics": {
			"$uid": {
				".read": "$uid === auth.uid",
		    	".write": "$uid === auth.uid"
			}
    }
  }
}
